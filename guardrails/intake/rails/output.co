define flow check intake output contract
  $allowed = execute check_intake_output_contract

  if not $allowed
    if $config.enable_rails_exceptions
      create event OutputRailException(message="Output not allowed. The deterministic intake output contract rail blocked the response.")
    else
      bot refuse to respond
    stop
